// Vite provides import.meta.env typings by default; no need to redeclare them
// However, if you want to add custom environment variables, you can extend the interface like this:
interface ImportMetaEnv {
  readonly VITE_API_URL: string; // Example of a custom environment variable
  // Add more custom environment variables here as needed
}   